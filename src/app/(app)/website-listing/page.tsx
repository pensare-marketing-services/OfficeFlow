'use client';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function WebsiteList() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Website Lists</CardTitle>
            </CardHeader>
            <CardContent>
                <p className='text-muted-foreground'>This dummy</p>
            </CardContent>
        </Card>
    )
}